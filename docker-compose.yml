services:
  controle_server:
    build:
      context: ./controle_server
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      # change to the .env.local to work localy
      - .env
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:15
    restart: always
    env_file:
      # change to the .env.local to work localy
      - .env
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

  nginx:
    image: nginx:latest
    ports:
      - "8080:80"
      - "4343:443"
    volumes:
      - ./nginx/admin_conf.d:/etc/nginx/conf.d
      - ./web_admin:/var/www/html
    depends_on:
      - controle_server

  adminer:
    image: adminer:latest
    restart: always
    ports:
      - "8081:8080"
    environment:
      ADMINER_DEFAULT_SERVER: postgres
    depends_on:
      - postgres
    profiles:
      - dev

volumes:
  pgdata:
    driver: local
