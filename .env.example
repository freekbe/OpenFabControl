# Copy this file to .env and fill values before running
# DB connection values for local development
POSTGRES_USER=ofc_user
POSTGRES_PASSWORD=change_me_secure_password
POSTGRES_DB=openfab
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Optionally override the server listen address
SERVER_ADDR=0.0.0.0:3000

# Mail (SMTP)
SMTP_HOST=smtp.mail.ovh.net
SMTP_PORT=465
SMTP_USERNAME=exemple@email.com
SMTP_PASSWORD=smtp_password
